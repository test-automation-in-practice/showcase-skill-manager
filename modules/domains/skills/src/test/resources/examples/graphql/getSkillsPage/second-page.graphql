query {
    getSkillsPage(pageIndex: 1, pageSize: 10) {
        content {
            id
            label
            description
            tags
        }
        pageIndex
        pageSize
        totalElements
        hasPrevious
        hasNext
    }
}
