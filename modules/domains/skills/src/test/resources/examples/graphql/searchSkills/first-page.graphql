query {
    searchSkills(query: "tags:language", pageIndex: 0, pageSize: 10) {
        content {
            id
            label
            description
            tags
        }
        pageIndex
        pageSize
        totalElements
        hasPrevious
        hasNext
    }
}
