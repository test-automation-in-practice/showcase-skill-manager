query {
    searchSkills(query: "tags:language", pagination: { index: 0, size: 10 }) {
        content {
            id
            label
            description
            tags
        }
        pageIndex
        pageSize
        totalElements
        hasPrevious
        hasNext
    }
}
