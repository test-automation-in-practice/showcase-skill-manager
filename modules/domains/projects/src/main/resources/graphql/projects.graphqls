type Project {
    id: ID!
    label: String!
    description: String!
}

type ProjectsPage {
    content: [Project]!
    pageIndex: Int!
    pageSize: Int!
    totalElements: Int!
    hasPrevious: Boolean!
    hasNext: Boolean!
}

extend type Query {
    getProjectById(id: ID!): Project
    getProjectsPage(index: Int, size: Int): ProjectsPage!
    searchProjects(query: String!, index: Int, size: Int): ProjectsPage!
    suggestProjects(input: String!, max: Int): [Suggestion]!
}

extend type Mutation {
    createProject(label: String!, description: String!) : Project!
    deleteProjectById(id: ID!) : Boolean!
}
