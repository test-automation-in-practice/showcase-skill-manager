query {
    searchEmployees(query: "name:doe", pageIndex: 1, pageSize: 10) {
        content {
            id
            firstName
            lastName
        }
        pageIndex
        pageSize
        totalElements
        hasPrevious
        hasNext
    }
}
