query {
    getEmployeesPage(pageIndex: 0, pageSize: 10) {
        content {
            id
            firstName
            lastName
        }
        pageIndex
        pageSize
        totalElements
        hasPrevious
        hasNext
    }
}
