query {
    searchEmployees(query: "name:doe", pageIndex: 0, pageSize: 10) {
        content {
            id
            firstName
            lastName
        }
        pageIndex
        pageSize
        totalElements
        hasPrevious
        hasNext
    }
}
